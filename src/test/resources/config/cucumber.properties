# ── Cucumber runtime options ──────────────────────────────────────────────────
# Suppress the "publish your results" banner
cucumber.publish.quiet=true

# Capture stdout from each step (visible in CI logs and the HTML report)
junit.platform.output.capture.stdout=true

# Clean console output (no ANSI colour codes in CI)
cucumber.ansi-colors.disabled=true